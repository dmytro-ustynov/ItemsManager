server {
    listen 80;

    # Serve the React app
    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        try_files $uri /index.html;
    }

    error_page 404 /404.html;
    location = /404.html {
        internal;
    }
}
